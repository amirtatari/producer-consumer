# producer-consumer
This is the implementation of producer-consumer pattern in C++20.

## How to build
After cloning the repo, go to the project directory and run the following commands:
```bash
mkdir build && cd build
cmake -DCMAKE_BUILD_TYPE=Release -S ..
cmake --build .
```

run the application using:
```bash
./tsafeQ
```
